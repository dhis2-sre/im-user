apiVersion: v1
kind: Service
metadata:
  name: {{ include "im-users.fullname" . }}
  labels:
    {{- include "im-users.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "im-users.selectorLabels" . | nindent 4 }}
