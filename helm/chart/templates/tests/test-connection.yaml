apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "im-users.fullname" . }}-test-connection"
  labels:
    {{- include "im-users.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "im-users.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
